name: Update Stock Radar Data

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Debug secrets
        run: |
          echo "VNSTOCK_API_KEY is set: ${{ secrets.VNSTOCK_API_KEY != '' }}"

      - name: Run scanner
        env:
          VNSTOCK_API_KEY: ${{ secrets.VNSTOCK_API_KEY }}
        run: npm run scan

      - name: Commit updated data
        run: |
          git config user.name "stock-radar-bot"
          git config user.email "stock-radar-bot@users.noreply.github.com"
          git add data.json
          git diff --staged --quiet || git commit -m "Update stock radar data"
          git push
